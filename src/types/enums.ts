export enum ENodeContentItemTypes {
  TEXT = "text",
  TEXTAREA = "textarea",
  DATE = "date",
  SELECT = "select",
}

export enum ENodeType {
  CALL_DURATION_LTE = "call_duration_lte",
  CALL_DURATION_GTE = "call_duration_gte",
  DIALOGUE_DATE_GTE = "dialogue_date_gte",
  DIALOGUE_DATE_LTE = "dialogue_date_lte",
  AUTOMATION_AGENT = "automation_agent",
  SELECTED_MARKER = "selected_marker",
}
